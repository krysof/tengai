# Tengai WebAssembly release

This branch contains browser build products only: HTML, JavaScript loader, WebAssembly, and normalized visual data.

Current scope: pure C++/WASM running visual slice. It is not yet the complete playable game.
No source CPU ROM, CPU emulator, AOT program, PC/opcode dispatcher, or C/C++ source is published here.
